/**
 * Module dependencies.
 */



/**
 * Expose `Form`.
 */

module.exports = Form;

/**
 * Initialize a new `Form` with a `schema` object
 * and prefill with `data` obj
 *
 * @param {Object} schema
 * @param {Object} data
 * @api public
 */

function Form(schema, data) {
  
}